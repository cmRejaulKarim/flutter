
import 'package:flutter/material.dart';

class AccountantDash extends StatelessWidget {
  const AccountantDash({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Hello Accountant"),
    );
  }
}
