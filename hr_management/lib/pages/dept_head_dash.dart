
import 'package:flutter/material.dart';

class DeptHeadDash extends StatelessWidget {
  const DeptHeadDash({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Hello Dept Head"),
    );
  }
}
